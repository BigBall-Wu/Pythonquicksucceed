---
title: "P99 【网络爬虫】代理ip介绍"
p_no: 99
category: 网络爬虫
duration: 403
bvid: BV1rpWjevEip
up: Python官方课程
type: learning-guide
created: 2026-06-24
updated: 2026-06-24
---

# P99 【网络爬虫】代理ip介绍

> **课程分类**：网络爬虫
> **时长**：403 秒
> **来源**：BV1rpWjevEip - Python官方课程

## 上下文

网络爬虫阶段，从 HTTP 基础到 requests 模块，再到综合案例。

## 课本对应章节

本节内容**书本无对应**，需用 Python 知识库补全。

## 一、课本内容（原书摘录）

> 本节内容书本未涵盖，跳过课本切片。


## 二、知识补全（书本没有的部分）

\
## 代理 IP 概念

### 为什么需要代理？

1. **访问限制**：某些网站限制地区访问
2. **IP 封禁**：爬虫太频繁被封 IP
3. **隐藏身份**：不想暴露真实 IP
4. **负载均衡**：分散请求到不同 IP

### 代理类型

| 类型 | 说明 | 速度 |
|------|------|------|
| HTTP 代理 | 仅代理 HTTP | 中 |
| HTTPS 代理 | 代理 HTTPS | 中 |
| SOCKS4/5 代理 | 通用代理 | 慢 |
| 透明代理 | 服务器知道你在用代理 | - |
| 匿名代理 | 服务器不知道 | - |
| 高匿代理 | 服务器认为你是普通用户 | - |

### 代理来源

- **免费代理**：质量差、不稳定
- **付费代理**：
  - 隧道代理：每次请求换 IP（如 阿布云、快代理）
  - 包时代理：固定 IP（如 蘑菇代理）
- **自建代理**：VPS 上搭建（推荐）

## 验证代理可用性

```python
import requests
import concurrent.futures

# 单个代理验证
def validate_proxy(proxy, timeout=5):
    """检查代理是否可用"""
    try:
        r = requests.get(
            "https://httpbin.org/ip",
            proxies={"http": proxy, "https": proxy},
            timeout=timeout
        )
        if r.status_code == 200:
            real_ip = r.json()["origin"]
            return {"proxy": proxy, "ip": real_ip, "ok": True}
    except Exception as e:
        return {"proxy": proxy, "ok": False, "error": str(e)}
    return {"proxy": proxy, "ok": False, "error": "unknown"}

# 批量验证
def validate_proxies(proxy_list, max_workers=10):
    """并发验证多个代理"""
    valid = []
    with concurrent.futures.ThreadPoolExecutor(max_workers=max_workers) as executor:
        futures = {executor.submit(validate_proxy, p): p for p in proxy_list}
        for future in concurrent.futures.as_completed(futures):
            result = future.result()
            if result["ok"]:
                valid.append(result)
    return valid

# 使用
proxies = [
    "http://1.1.1.1:8080",
    "http://2.2.2.2:8080",
    "http://3.3.3.3:8080",
]
valid_proxies = validate_proxies(proxies)
print(f"可用代理：{len(valid_proxies)}/{len(proxies)}")
for p in valid_proxies:
    print(f"  {p['proxy']} -> IP {p['ip']}")
```

## 免费代理源（学习用）

```
# https://www.free-proxy-list.net/
# https://www.proxyscrape.com/
# https://github.com/clarketm/proxylist
```

但生产环境务必用付费代理（隧道代理推荐），免费代理可用率<10%。

## 三、动手练习

- [ ] 看完本节视频
- [ ] 自己手写一遍示例代码
- [ ] 完成课本对应章节练习（如果有）
- [ ] 尝试修改示例，做小实验
- [ ] 整理笔记（用 WikiLink 链接到相关 Vault 笔记）